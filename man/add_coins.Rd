% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coins_generation.R
\name{add_coins}
\alias{add_coins}
\title{Generate and append COinS metadata to a Quarto post}
\usage{
add_coins(backup = TRUE)
}
\arguments{
\item{backup}{Logical. Create a \code{.bak} backup before modifying the file
(default \code{TRUE}). Recommended for peace of mind during development.}
}
\value{
Invisibly returns the generated COinS chunk as a character string.
Messages and the chunk code are printed to the console.
}
\description{
Automatically extracts bibliographic metadata from a Quarto post's YAML
front matter and injects it as a COinS (ContextObjects in Spans) metadata
chunk. COinS enables bibliographic tools like Zotero to detect and import
citation information from rendered HTML.
}
\details{
\subsection{Setup}{

To use \code{add_coins()} effectively, configure your project's \code{.Rprofile}
(at the same level as \verb{_quarto.yml}) with:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{options(
  qpost.lang    = "en",
  qpost.license = "CC BY 4.0"
)
}\if{html}{\out{</div>}}

Then restart your R session. These settings provide defaults for the
\code{lang} and \code{license} fields, which can be overridden per-post.
}

\subsection{Auto-resolution of Metadata Fields}{

Four YAML fields are automatically resolved in this priority order:
\enumerate{
\item Value in the document's YAML header (if present and non-empty)
\item Derived from \verb{_quarto.yml} (\code{blog-title} and \code{url} only)
\item From \code{.Rprofile} options (\code{lang} and \code{license} only)
\item Omitted if not found anywhere
}

The required fields \code{title} and \code{date} must be present in the YAML header.
}

\subsection{Generated Output}{

The function appends a fenced R code cell (\code{{r}} block) with output
rendered as a hidden \verb{<span class="Z3988">} containing the COinS query
string. This metadata is machine-readable but invisible in the browser.

If a COinS chunk already exists (detected by the label \code{coins-code}),
the user is prompted to confirm overwrite, and a \code{.bak} backup is created.
}
}
\examples{
\dontrun{
# From an RStudio editor with a .qmd file open:
add_coins()

# To skip backup:
add_coins(backup = FALSE)

# To manually process a file path (no RStudio required):
generate_and_append_coins("path/to/post.qmd")
}
}
